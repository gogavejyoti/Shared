 // Restore cross-sheet references
    if(e.ctrlValue && e.ctrlValue.crossSheetRefs) {
        _shiftCrossSheetReference({
            type: e.type == "addRC" ? "deleteRow" : "insertRow", // reverse operation
            sheetIndex: e.sheetIndex,
            rowStart: e.ctrlValue.rowStart,
            rowEnd: e.ctrlValue.rowEnd,
            colStart: e.ctrlValue.colStart,
            colEnd: e.ctrlValue.colEnd,
            formulas: e.ctrlValue.crossSheetRefs // previous formulas to restore
        });
    }
