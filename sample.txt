else if (n.type == "addRC") {
    let l = $.extend(!0, {}, n.ctrlValue);
    l.direction == "rightbottom" && (l.index = l.index + 1),
    Bn(n.data, n.config, "delRC", l, n.calc, n.filterObj, n.cf, n.af, n.freezen, n.dataVerification, n.hyperlink);

    // Redo cross-sheet references
    if(l.crossSheetRefs){
        _shiftCrossSheetReference({
            type: "insertRow", // same as original add
            sheetIndex: n.sheetIndex,
            rowStart: l.rowStart,
            rowEnd: l.rowEnd,
            colStart: l.colStart,
            colEnd: l.colEnd,
            formulas: l.crossSheetRefs
        });
    }
}
else if (n.type == "delRC") {
    let l = $.extend(!0, {}, n.ctrlValue);
    l.restore = !0;
    l.direction = "lefttop",
    Bn(n.data, n.config, "addRC", l, n.calc, n.filterObj, n.cf, n.af, n.freezen, n.dataVerification, n.hyperlink);

    // Redo cross-sheet references
    if(l.crossSheetRefs){
        _shiftCrossSheetReference({
            type: "deleteRow", // same as original delete
            sheetIndex: n.sheetIndex,
            rowStart: l.rowStart,
            rowEnd: l.rowEnd,
            colStart: l.colStart,
            colEnd: l.colEnd,
            formulas: l.crossSheetRefs
        });
    }
}
