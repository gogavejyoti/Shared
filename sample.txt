 // 🔹 Adjust cross-sheet references after insertion
    if (typeof _shiftCrossSheetReference === "function") {
        _shiftCrossSheetReference({
            type: "insertRow",
            sheetIndex: h.currentSheetIndex,
            rowIndex: n,   // inserting above this row
            rowCount: 1
        });
    }
